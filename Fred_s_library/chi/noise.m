function[nois]= noise(k_obs)% This function calculates the noise spectrum at given k values. % Currently, the noise spectrum is assumed constant at a value of 2.0x 10^-6.%% INPUTS:k_obs->This is a vector of k values from the observed spectrum data.%			   	The vector is x by 1 in size, where x is the number of k values.%				It is used to provide the correct length for the noise vector.%% OUTPUTS:nois-> A vector of noise spectrum values. The vector is x by 1 %					in size, where x is the number of k values.%% USAGE:   noise_array=noise(k_obs);%%% Fred rewrote in a easier form (March 2012)%% Constant noise level used after fabricant specs:% http://www.rocklandscientific.com/Products/Sensors/FP0738/tabid/95/Default.aspx%n=3.0E-7;        %set the constant noise value.n = 1e-6;%n = 1e-13;nois(1:length(k_obs)) = n;% --------------- FC comments ----------------- %% $$$ noise_k=n;          %initialize the noise vector.% $$$ loop=length(k_obs)-1;% $$$ for i=1:loop         %make the noise vector. % $$$    noise_k=[noise_k, n];% $$$ end% $$$ % $$$ nois=noise_k;          %return the noise vector.% $$$ % $$$  % ---------------------------------------------- %